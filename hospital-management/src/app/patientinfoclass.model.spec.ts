import { Patientinfoclass } from './patientinfoclass.model';

describe('Patientinfoclass', () => {
  it('should create an instance', () => {
    expect(new Patientinfoclass()).toBeTruthy();
  });
});
